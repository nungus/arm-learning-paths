[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smolvla-executorch-arm"
version = "0.1.0"
description = "Learning-path workflow for SmolVLA on ExecuTorch/XNNPACK"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]

